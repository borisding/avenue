## `session` table for database
## run the following SQL for creating session table:

CREATE TABLE IF NOT EXISTS `session` (
    `id` VARCHAR(32) NOT NULL,
    `value` text,
    `timestamp` int(11) NOT NULL,
    PRIMARY KEY (`id`),
    INDEX (`timestamp`)
) ENGINE=InnoDB;